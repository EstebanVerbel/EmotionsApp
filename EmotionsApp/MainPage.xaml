<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:EmotionsApp"
             x:Class="EmotionsApp.MainPage">
    <ContentPage.Content>

        <ScrollView>

            <StackLayout>

                <Button x:Name="takePhoto" Text="Take Photo" />

                <Image x:Name="image"
                       HorizontalOptions="FillAndExpand"
                       VerticalOptions="FillAndExpand" />

                <ActivityIndicator x:Name="IsLoading" 
                                   IsRunning="True"
                                   IsVisible="False" />

                <Button Text="Analize" 
                        Clicked="Button_Clicked" />

            </StackLayout>
            
        </ScrollView>

    </ContentPage.Content>
</ContentPage>